📁 File Organizer (Консольный органайзер файлов)

Этот скрипт автоматически сортирует файлы в указанной директории по категориям: изображения, видео, документы, архивы, аудио, код, базы данных, шрифты и другие.
Также поддерживается переименование файлов с добавлением даты создания и режим «сухого прогона».

⸻

🔧 Возможности
	•	Автоматическая сортировка файлов по подпапкам на основе расширений
	•	Режим сухого прогона (ничего не изменяется — только показывается, что будет сделано)
	•	Опциональное переименование файлов с добавлением даты создания
	•	Фильтрация недопустимых символов в именах
	•	Автоматическое создание папок категорий
	•	Разрешение конфликтов имён с автоматическим добавлением _copy1, _copy2, …
	•	Подробный или тихий режим

⸻

📦 Категории файлов

Файлы распределяются по папкам:
	•	Images — jpg, png, gif, svg, webp, tiff, bmp …
	•	Videos — mp4, mov, avi, mkv …
	•	Documents — pdf, docx, xlsx, pptx, txt, csv …
	•	Audio — mp3, wav, flac …
	•	Archives — zip, rar, 7z, tar, gz …
	•	Executables — exe, msi, dmg, deb …
	•	Code — py, js, html, css, json, sh, cpp …
	•	Databases — db, sqlite
	•	Fonts — ttf, otf, woff …
	•	Other — всё остальное
	•	No_Extension — файлы без расширения

  🚀 Установка
  ```
  git clone https://github.com/DizzyZ7/file-organizer.git
```
cd file-organizer
Скрипт не требует сторонних библиотек — работает на стандартных модулях Python.

▶️ Использование

Базовый запуск (сортировка текущей директории)
```
python organizer.py
```
Указание пути
```
python organizer.py --path
"C:/Users/Me/Downloads"
```
Сухой прогон (без изменений)
```
python organizer.py --dry-run
```
Переименование файлов с датой создания
```
python organizer.py --rename-date
```
Тихий режим (без вывода действий)
```
python organizer.py --silent
```
Комбинации
```
python organizer.py --path ~/Downloads --rename-date --dry-run
```
🧩 Примеры того, что делает скрипт
	•	photo.png → Images/photo.png
	•	report.docx → Documents/report.docx
	•	С включённым --rename-date:
report.docx → Documents/report_2024-01-15.docx
	•	Если файл уже существует:
photo.png → Images/photo_copy1.png

📁 Структура проекта
file-organizer/
│── organizer.py     # Основной скрипт
└── README.md
